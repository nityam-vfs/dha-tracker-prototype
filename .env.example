# Optional: Supabase configuration.
# If these are NOT set, the app automatically falls back to the built-in mock
# data in lib/mockData.js, so the prototype runs locally without any setup.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
